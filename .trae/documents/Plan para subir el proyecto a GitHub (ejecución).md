## Alcance
- Preparar el repo local y commit inicial.
- Mantener higiene: añadir `.gitignore` para Node/TS.
- Dejar listo para empujar a `tony2688/desafio_laburen` con instrucciones de push seguras.

## Pasos
1. Añadir `.gitignore` (excluir `node_modules`, `dist`, `.env`, etc.).
2. Inicializar repo git, crear branch `phase1-initial-skeleton`, commit inicial.
3. Instrucciones para configurar remoto y hacer `push` al repo de GitHub.

## Resultado esperado
- Repo local inicializado en `c:\Users\PC\Desktop\desafio_laburen` con branch `phase1-initial-skeleton` y commit.
- Lista de comandos para que empujes a GitHub con tus credenciales.